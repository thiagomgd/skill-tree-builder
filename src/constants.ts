/**
 * Application-wide constants
 */

/**
 * Node type identifier for skill nodes.
 */
export const NODE_TYPE = "skillNode";

/**
 * localStorage key for persisting skill tree data.
 */
export const STORAGE_KEY = "skillTree";

/**
 * Debounce delay (in milliseconds) for saving to localStorage.
 */
export const SAVE_DEBOUNCE_MS = 500;

/**
 * New node placement constants
 */
export const NEW_NODE_OFFSET = 100;
export const NEW_NODE_X_RANGE = 300;
export const NEW_NODE_Y_RANGE = 200;
